#ifndef __tile_hpp
#define __tile_hpp

#include <vector>
#include <iostream>

#include <boost/filesystem.hpp>

#include <png.h>

#include <json_spirit.h>

class tile
{
public:
  tile(arguments);
  ~tile();

  /* data */
};

class tile_cache
{
public:
  tile_cache(arguments);
  ~tile_cache();

  /* data */
};

#endif
